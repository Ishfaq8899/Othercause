% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Other_cause_death.R, R/try_other_cause_death.R
\name{filter_multiple_cancers}
\alias{filter_multiple_cancers}
\title{Filter Data for Multiple Cancer Sites}
\usage{
filter_multiple_cancers(data, cancer_sites)

filter_multiple_cancers(data, cancer_sites)
}
\arguments{
\item{data}{A data frame containing MCED cancer mortality data.
It should include a column named `cancer_site`, which may be created by mapping
values from the `ICD-10 113 Cause List` column to standardized cancer site names.}

\item{cancer_sites}{A vector of selected cancer type names to filter. These names
should match the values in the `ICD-10 113 Cause List` column of
the data frame.}
}
\value{
A filtered data frame containing only records where cancer_site
  matches one of the specified sites.

A data frame containing only rows where cancer type is in `cancer_sites`.
}
\description{
Filters a dataset to include only records for specified cancer sites.

This function filters the input MCED cancer data to include only the cancer types
specified in the `cancer_sites` vector. It uses the `filter` function from `dplyr`
to select rows where the cancer type matches any of the types listed in `cancer_sites`.
}
\examples{
\dontrun{
# Filter for lung and breast cancers
filtered_data <- filter_multiple_cancers(
  data = cancer_data,
  cancer_sites = c("Lung", "Breast")
)
}

selected_cancers <- c(
"Malignant neoplasm of prostate (C61)",
"Malignant neoplasm of bladder (C67)"
)
filtered_data <- filter_multiple_cancers(MCED_cdc, selected_cancers)




}
